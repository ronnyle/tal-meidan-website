# tal-meidan-website

install and compile:
```
gem install bundler
bundle install
npm install
npm run build:css
```

run:
```
bundle exec jekyll serve --livereload
```

view:
```
http://localhost:4000
```