# tal-meidan-website

install and compile:
```
gem install bundler
bundle install
npm install
npx tailwindcss -i ./css/style.css -o ./assets/css/main.css --minify
```

run:
```
bundle exec jekyll serve --livereload
```

view:
```
http://localhost:4000
```